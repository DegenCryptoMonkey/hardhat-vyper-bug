### Installation

Run the following command to install dependencies and compile the contracts:

```shell
$ npm install
$ npm run compile
```

### Command to reproduce bug

```shell
$ npm run reproduce
```